StylesPath = /tmp/.vale/styles

MinAlertLevel = error

[*]
BasedOnStyles = Vale

# Proposals are design documents that necessarily carry technical jargon (tool
# names, API fields, plurals) that Vale's built-in dictionary rejects. Keep the
# other Vale rules, but do not spell-check proposals.
[proposals/**/*.md]
Vale.Spelling = NO
